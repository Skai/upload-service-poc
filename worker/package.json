{
  "name": "uploads-worker",
  "version": "0.1.0",
  "author": "Julie Ng",
  "scripts": {
    "babel": "babel-node src/index.js --presets es2015",
    "dev": "nodemon -w src --exec \"babel-node src/index.js --presets es2015\"",
    "compile": "babel src --out-dir dist --presets es2015"
  },
  "dependencies": {
    "busboy": "^0.2.14",
    "express": "^4.14.0",
    "webstomp-client": "^1.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "nodemon": "^1.11.0"
  }
}
